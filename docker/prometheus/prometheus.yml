global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "lexcorp-api"
    metrics_path: /api/v1/health/metrics
    static_configs:
      - targets: ["api:9100"]
        labels:
          service: "lexcorp"
          component: "api"
